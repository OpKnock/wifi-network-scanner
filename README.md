# WiFi Network Scanner

Passive 802.11 beacon/probe analysis toolkit (pure offline logic): parses
beacon frames, extracts SSID/BSSID/channel/security, and classifies networks.
Educational only - no radio access required.

Run tests: `python -m pytest`
